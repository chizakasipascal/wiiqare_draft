import 'package:flutter/material.dart';

class Consiltation extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Consiltation"),
    );
  }
}
