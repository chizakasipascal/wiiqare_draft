import 'package:flutter/material.dart';

class LaboTabs extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text("Labo"),
      ),
    );
  }
}
